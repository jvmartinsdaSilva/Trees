#include "BiTree.h"
#include <stdlib.h>
#include <stdio.h>

int main(){
    BiTree biTree;
    biTree.insertValue(5);
    biTree.insertValue(7);
    biTree.insertValue(9);
    biTree.searchValue(7);
}